var xi = Object.defineProperty;
var Oi = (h, s, t) => s in h ? xi(h, s, { enumerable: !0, configurable: !0, writable: !0, value: t }) : h[s] = t;
var g = (h, s, t) => (Oi(h, typeof s != "symbol" ? s + "" : s, t), t);
class Hi {
  constructor() {
    g(this, "listeners");
    this.listeners = /* @__PURE__ */ new Map([
      ["drawStart", /* @__PURE__ */ new Set()],
      ["drawUpdate", /* @__PURE__ */ new Set()],
      ["drawEnd", /* @__PURE__ */ new Set()],
      ["editStart", /* @__PURE__ */ new Set()],
      ["editEnd", /* @__PURE__ */ new Set()]
    ]);
  }
  on(s, t) {
    if (!this.listeners.has(s)) {
      console.warn("Event binding must be one of 'drawStart', 'drawUpdate', or 'drawEnd'.");
      return;
    }
    this.listeners.get(s).add(t);
  }
  off(s, t) {
    this.listeners.has(s) && this.listeners.get(s).delete(t);
  }
  dispatchEvent(s, t) {
    this.listeners.has(s) && this.listeners.get(s).forEach((e) => {
      e(t);
    });
  }
}
var rt = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Ri(h) {
  return h && h.__esModule && Object.prototype.hasOwnProperty.call(h, "default") ? h.default : h;
}
var at = { exports: {} };
at.exports;
(function(h, s) {
  var t = 200, e = "__lodash_hash_undefined__", i = 9007199254740991, o = "[object Arguments]", r = "[object Array]", a = "[object Boolean]", l = "[object Date]", c = "[object Error]", p = "[object Function]", f = "[object GeneratorFunction]", m = "[object Map]", v = "[object Number]", P = "[object Object]", w = "[object Promise]", M = "[object RegExp]", S = "[object Set]", C = "[object String]", F = "[object Symbol]", G = "[object WeakMap]", D = "[object ArrayBuffer]", b = "[object DataView]", $ = "[object Float32Array]", Ft = "[object Float64Array]", bt = "[object Int8Array]", _t = "[object Int16Array]", kt = "[object Int32Array]", Gt = "[object Uint8Array]", It = "[object Uint8ClampedArray]", Dt = "[object Uint16Array]", Wt = "[object Uint32Array]", de = /[\\^$.*+?()[\]{}|]/g, ge = /\w*$/, pe = /^\[object .+?Constructor\]$/, fe = /^(?:0|[1-9]\d*)$/, E = {};
  E[o] = E[r] = E[D] = E[b] = E[a] = E[l] = E[$] = E[Ft] = E[bt] = E[_t] = E[kt] = E[m] = E[v] = E[P] = E[M] = E[S] = E[C] = E[F] = E[Gt] = E[It] = E[Dt] = E[Wt] = !0, E[c] = E[p] = E[G] = !1;
  var Pe = typeof rt == "object" && rt && rt.Object === Object && rt, me = typeof self == "object" && self && self.Object === Object && self, H = Pe || me || Function("return this")(), xt = s && !s.nodeType && s, Ot = xt && !0 && h && !h.nodeType && h, ye = Ot && Ot.exports === xt;
  function we(n, u) {
    return n.set(u[0], u[1]), n;
  }
  function ve(n, u) {
    return n.add(u), n;
  }
  function Te(n, u) {
    for (var d = -1, y = n ? n.length : 0; ++d < y && u(n[d], d, n) !== !1; )
      ;
    return n;
  }
  function Me(n, u) {
    for (var d = -1, y = u.length, L = n.length; ++d < y; )
      n[L + d] = u[d];
    return n;
  }
  function Ht(n, u, d, y) {
    var L = -1, _ = n ? n.length : 0;
    for (y && _ && (d = n[++L]); ++L < _; )
      d = u(d, n[L], L, n);
    return d;
  }
  function Se(n, u) {
    for (var d = -1, y = Array(n); ++d < n; )
      y[d] = u(d);
    return y;
  }
  function Ce(n, u) {
    return n == null ? void 0 : n[u];
  }
  function Rt(n) {
    var u = !1;
    if (n != null && typeof n.toString != "function")
      try {
        u = !!(n + "");
      } catch {
      }
    return u;
  }
  function jt(n) {
    var u = -1, d = Array(n.size);
    return n.forEach(function(y, L) {
      d[++u] = [L, y];
    }), d;
  }
  function ct(n, u) {
    return function(d) {
      return n(u(d));
    };
  }
  function Bt(n) {
    var u = -1, d = Array(n.size);
    return n.forEach(function(y) {
      d[++u] = y;
    }), d;
  }
  var Ee = Array.prototype, Ae = Function.prototype, tt = Object.prototype, lt = H["__core-js_shared__"], Ut = function() {
    var n = /[^.]+$/.exec(lt && lt.keys && lt.keys.IE_PROTO || "");
    return n ? "Symbol(src)_1." + n : "";
  }(), Vt = Ae.toString, j = tt.hasOwnProperty, et = tt.toString, Le = RegExp(
    "^" + Vt.call(j).replace(de, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
  ), Nt = ye ? H.Buffer : void 0, qt = H.Symbol, Xt = H.Uint8Array, Fe = ct(Object.getPrototypeOf, Object), be = Object.create, _e = tt.propertyIsEnumerable, ke = Ee.splice, Yt = Object.getOwnPropertySymbols, Ge = Nt ? Nt.isBuffer : void 0, Ie = ct(Object.keys, Object), ut = K(H, "DataView"), J = K(H, "Map"), dt = K(H, "Promise"), gt = K(H, "Set"), pt = K(H, "WeakMap"), Q = K(Object, "create"), De = N(ut), We = N(J), xe = N(dt), Oe = N(gt), He = N(pt), Kt = qt ? qt.prototype : void 0, zt = Kt ? Kt.valueOf : void 0;
  function U(n) {
    var u = -1, d = n ? n.length : 0;
    for (this.clear(); ++u < d; ) {
      var y = n[u];
      this.set(y[0], y[1]);
    }
  }
  function Re() {
    this.__data__ = Q ? Q(null) : {};
  }
  function je(n) {
    return this.has(n) && delete this.__data__[n];
  }
  function Be(n) {
    var u = this.__data__;
    if (Q) {
      var d = u[n];
      return d === e ? void 0 : d;
    }
    return j.call(u, n) ? u[n] : void 0;
  }
  function Ue(n) {
    var u = this.__data__;
    return Q ? u[n] !== void 0 : j.call(u, n);
  }
  function Ve(n, u) {
    var d = this.__data__;
    return d[n] = Q && u === void 0 ? e : u, this;
  }
  U.prototype.clear = Re, U.prototype.delete = je, U.prototype.get = Be, U.prototype.has = Ue, U.prototype.set = Ve;
  function R(n) {
    var u = -1, d = n ? n.length : 0;
    for (this.clear(); ++u < d; ) {
      var y = n[u];
      this.set(y[0], y[1]);
    }
  }
  function Ne() {
    this.__data__ = [];
  }
  function qe(n) {
    var u = this.__data__, d = it(u, n);
    if (d < 0)
      return !1;
    var y = u.length - 1;
    return d == y ? u.pop() : ke.call(u, d, 1), !0;
  }
  function Xe(n) {
    var u = this.__data__, d = it(u, n);
    return d < 0 ? void 0 : u[d][1];
  }
  function Ye(n) {
    return it(this.__data__, n) > -1;
  }
  function Ke(n, u) {
    var d = this.__data__, y = it(d, n);
    return y < 0 ? d.push([n, u]) : d[y][1] = u, this;
  }
  R.prototype.clear = Ne, R.prototype.delete = qe, R.prototype.get = Xe, R.prototype.has = Ye, R.prototype.set = Ke;
  function X(n) {
    var u = -1, d = n ? n.length : 0;
    for (this.clear(); ++u < d; ) {
      var y = n[u];
      this.set(y[0], y[1]);
    }
  }
  function ze() {
    this.__data__ = {
      hash: new U(),
      map: new (J || R)(),
      string: new U()
    };
  }
  function $e(n) {
    return nt(this, n).delete(n);
  }
  function Je(n) {
    return nt(this, n).get(n);
  }
  function Qe(n) {
    return nt(this, n).has(n);
  }
  function Ze(n, u) {
    return nt(this, n).set(n, u), this;
  }
  X.prototype.clear = ze, X.prototype.delete = $e, X.prototype.get = Je, X.prototype.has = Qe, X.prototype.set = Ze;
  function Y(n) {
    this.__data__ = new R(n);
  }
  function ti() {
    this.__data__ = new R();
  }
  function ei(n) {
    return this.__data__.delete(n);
  }
  function ii(n) {
    return this.__data__.get(n);
  }
  function ni(n) {
    return this.__data__.has(n);
  }
  function si(n, u) {
    var d = this.__data__;
    if (d instanceof R) {
      var y = d.__data__;
      if (!J || y.length < t - 1)
        return y.push([n, u]), this;
      d = this.__data__ = new X(y);
    }
    return d.set(n, u), this;
  }
  Y.prototype.clear = ti, Y.prototype.delete = ei, Y.prototype.get = ii, Y.prototype.has = ni, Y.prototype.set = si;
  function oi(n, u) {
    var d = mt(n) || bi(n) ? Se(n.length, String) : [], y = d.length, L = !!y;
    for (var _ in n)
      (u || j.call(n, _)) && !(L && (_ == "length" || Ei(_, y))) && d.push(_);
    return d;
  }
  function $t(n, u, d) {
    var y = n[u];
    (!(j.call(n, u) && te(y, d)) || d === void 0 && !(u in n)) && (n[u] = d);
  }
  function it(n, u) {
    for (var d = n.length; d--; )
      if (te(n[d][0], u))
        return d;
    return -1;
  }
  function ri(n, u) {
    return n && Jt(u, yt(u), n);
  }
  function ft(n, u, d, y, L, _, W) {
    var I;
    if (y && (I = _ ? y(n, L, _, W) : y(n)), I !== void 0)
      return I;
    if (!st(n))
      return n;
    var ne = mt(n);
    if (ne) {
      if (I = Mi(n), !u)
        return wi(n, I);
    } else {
      var z = V(n), se = z == p || z == f;
      if (ki(n))
        return di(n, u);
      if (z == P || z == o || se && !_) {
        if (Rt(n))
          return _ ? n : {};
        if (I = Si(se ? {} : n), !u)
          return vi(n, ri(I, n));
      } else {
        if (!E[z])
          return _ ? n : {};
        I = Ci(n, z, ft, u);
      }
    }
    W || (W = new Y());
    var oe = W.get(n);
    if (oe)
      return oe;
    if (W.set(n, I), !ne)
      var re = d ? Ti(n) : yt(n);
    return Te(re || n, function(wt, ot) {
      re && (ot = wt, wt = n[ot]), $t(I, ot, ft(wt, u, d, y, ot, n, W));
    }), I;
  }
  function ai(n) {
    return st(n) ? be(n) : {};
  }
  function hi(n, u, d) {
    var y = u(n);
    return mt(n) ? y : Me(y, d(n));
  }
  function ci(n) {
    return et.call(n);
  }
  function li(n) {
    if (!st(n) || Li(n))
      return !1;
    var u = ie(n) || Rt(n) ? Le : pe;
    return u.test(N(n));
  }
  function ui(n) {
    if (!Zt(n))
      return Ie(n);
    var u = [];
    for (var d in Object(n))
      j.call(n, d) && d != "constructor" && u.push(d);
    return u;
  }
  function di(n, u) {
    if (u)
      return n.slice();
    var d = new n.constructor(n.length);
    return n.copy(d), d;
  }
  function Pt(n) {
    var u = new n.constructor(n.byteLength);
    return new Xt(u).set(new Xt(n)), u;
  }
  function gi(n, u) {
    var d = u ? Pt(n.buffer) : n.buffer;
    return new n.constructor(d, n.byteOffset, n.byteLength);
  }
  function pi(n, u, d) {
    var y = u ? d(jt(n), !0) : jt(n);
    return Ht(y, we, new n.constructor());
  }
  function fi(n) {
    var u = new n.constructor(n.source, ge.exec(n));
    return u.lastIndex = n.lastIndex, u;
  }
  function Pi(n, u, d) {
    var y = u ? d(Bt(n), !0) : Bt(n);
    return Ht(y, ve, new n.constructor());
  }
  function mi(n) {
    return zt ? Object(zt.call(n)) : {};
  }
  function yi(n, u) {
    var d = u ? Pt(n.buffer) : n.buffer;
    return new n.constructor(d, n.byteOffset, n.length);
  }
  function wi(n, u) {
    var d = -1, y = n.length;
    for (u || (u = Array(y)); ++d < y; )
      u[d] = n[d];
    return u;
  }
  function Jt(n, u, d, y) {
    d || (d = {});
    for (var L = -1, _ = u.length; ++L < _; ) {
      var W = u[L], I = y ? y(d[W], n[W], W, d, n) : void 0;
      $t(d, W, I === void 0 ? n[W] : I);
    }
    return d;
  }
  function vi(n, u) {
    return Jt(n, Qt(n), u);
  }
  function Ti(n) {
    return hi(n, yt, Qt);
  }
  function nt(n, u) {
    var d = n.__data__;
    return Ai(u) ? d[typeof u == "string" ? "string" : "hash"] : d.map;
  }
  function K(n, u) {
    var d = Ce(n, u);
    return li(d) ? d : void 0;
  }
  var Qt = Yt ? ct(Yt, Object) : Di, V = ci;
  (ut && V(new ut(new ArrayBuffer(1))) != b || J && V(new J()) != m || dt && V(dt.resolve()) != w || gt && V(new gt()) != S || pt && V(new pt()) != G) && (V = function(n) {
    var u = et.call(n), d = u == P ? n.constructor : void 0, y = d ? N(d) : void 0;
    if (y)
      switch (y) {
        case De:
          return b;
        case We:
          return m;
        case xe:
          return w;
        case Oe:
          return S;
        case He:
          return G;
      }
    return u;
  });
  function Mi(n) {
    var u = n.length, d = n.constructor(u);
    return u && typeof n[0] == "string" && j.call(n, "index") && (d.index = n.index, d.input = n.input), d;
  }
  function Si(n) {
    return typeof n.constructor == "function" && !Zt(n) ? ai(Fe(n)) : {};
  }
  function Ci(n, u, d, y) {
    var L = n.constructor;
    switch (u) {
      case D:
        return Pt(n);
      case a:
      case l:
        return new L(+n);
      case b:
        return gi(n, y);
      case $:
      case Ft:
      case bt:
      case _t:
      case kt:
      case Gt:
      case It:
      case Dt:
      case Wt:
        return yi(n, y);
      case m:
        return pi(n, y, d);
      case v:
      case C:
        return new L(n);
      case M:
        return fi(n);
      case S:
        return Pi(n, y, d);
      case F:
        return mi(n);
    }
  }
  function Ei(n, u) {
    return u = u ?? i, !!u && (typeof n == "number" || fe.test(n)) && n > -1 && n % 1 == 0 && n < u;
  }
  function Ai(n) {
    var u = typeof n;
    return u == "string" || u == "number" || u == "symbol" || u == "boolean" ? n !== "__proto__" : n === null;
  }
  function Li(n) {
    return !!Ut && Ut in n;
  }
  function Zt(n) {
    var u = n && n.constructor, d = typeof u == "function" && u.prototype || tt;
    return n === d;
  }
  function N(n) {
    if (n != null) {
      try {
        return Vt.call(n);
      } catch {
      }
      try {
        return n + "";
      } catch {
      }
    }
    return "";
  }
  function Fi(n) {
    return ft(n, !0, !0);
  }
  function te(n, u) {
    return n === u || n !== n && u !== u;
  }
  function bi(n) {
    return _i(n) && j.call(n, "callee") && (!_e.call(n, "callee") || et.call(n) == o);
  }
  var mt = Array.isArray;
  function ee(n) {
    return n != null && Gi(n.length) && !ie(n);
  }
  function _i(n) {
    return Ii(n) && ee(n);
  }
  var ki = Ge || Wi;
  function ie(n) {
    var u = st(n) ? et.call(n) : "";
    return u == p || u == f;
  }
  function Gi(n) {
    return typeof n == "number" && n > -1 && n % 1 == 0 && n <= i;
  }
  function st(n) {
    var u = typeof n;
    return !!n && (u == "object" || u == "function");
  }
  function Ii(n) {
    return !!n && typeof n == "object";
  }
  function yt(n) {
    return ee(n) ? oi(n) : ui(n);
  }
  function Di() {
    return [];
  }
  function Wi() {
    return !1;
  }
  h.exports = Fi;
})(at, at.exports);
var ji = at.exports;
const Bi = /* @__PURE__ */ Ri(ji), ae = 100, St = 1e-4, A = (h, s) => Math.sqrt((h[0] - s[0]) ** 2 + (h[1] - s[1]) ** 2), ht = (h) => {
  let s = 0;
  return h && Array.isArray(h) && h.length > 0 && h.forEach((t, e) => {
    e < h.length - 1 && (s += A(t, h[e + 1]));
  }), s;
}, x = (h) => ht(h) ** 0.99, O = (h, s) => [(h[0] + s[0]) / 2, (h[1] + s[1]) / 2], Ui = (h, s, t) => {
  const e = [(h[0] + s[0]) / 2, (h[1] + s[1]) / 2], i = [e[0] - h[1] + s[1], e[1] + h[0] - s[0]], o = [(h[0] + t[0]) / 2, (h[1] + t[1]) / 2], r = [o[0] - h[1] + t[1], o[1] + h[0] - t[0]];
  return Vi(e, i, o, r);
}, Vi = (h, s, t, e) => {
  if (h[1] === s[1]) {
    const c = (e[0] - t[0]) / (e[1] - t[1]) * (h[1] - t[1]) + t[0], p = h[1];
    return [c, p];
  }
  if (t[1] === e[1]) {
    const c = (s[0] - h[0]) / (s[1] - h[1]) * (t[1] - h[1]) + h[0], p = t[1];
    return [c, p];
  }
  const i = (s[0] - h[0]) / (s[1] - h[1]), o = (e[0] - t[0]) / (e[1] - t[1]), r = (i * h[1] - h[0] - o * t[1] + t[0]) / (i - o);
  return [i * r - i * h[1] + h[0], r];
}, q = (h, s) => {
  let t;
  const e = Math.asin(Math.abs(s[1] - h[1]) / A(h, s));
  return s[1] >= h[1] && s[0] >= h[0] ? t = e + Math.PI : s[1] >= h[1] && s[0] < h[0] ? t = Math.PI * 2 - e : s[1] < h[1] && s[0] < h[0] ? t = e : s[1] < h[1] && s[0] >= h[0] && (t = Math.PI - e), t;
}, Mt = (h, s, t) => {
  const e = q(s, h) - q(s, t);
  return e < 0 ? e + Math.PI * 2 : e;
}, Z = (h, s, t) => (t[1] - h[1]) * (s[0] - h[0]) > (s[1] - h[1]) * (t[0] - h[0]), Ni = (h, s, t, e, i) => {
  h = Math.max(Math.min(h, 1), 0);
  const [o, r] = [1 - h, h * h], a = r * h, l = o * o, c = l * o, p = c * s[0] + 3 * l * h * t[0] + 3 * o * r * e[0] + a * i[0], f = c * s[1] + 3 * l * h * t[1] + 3 * o * r * e[1] + a * i[1];
  return [p, f];
}, T = (h, s, t, e, i) => {
  const o = q(h, s), r = i ? o + t : o - t, a = e * Math.cos(r), l = e * Math.sin(r);
  return [s[0] + a, s[1] + l];
}, ce = (h, s, t, e) => {
  let [i, o, r, a] = [null, null, [], e - t];
  a = a < 0 ? a + Math.PI * 2 : a;
  for (let l = 0; l <= 100; l++) {
    const c = t + a * l / 100;
    i = h[0] + s * Math.cos(c), o = h[1] + s * Math.sin(c), r.push([i, o]);
  }
  return r;
}, Ct = (h, s, t, e) => {
  const i = Et(s, t, e);
  let [o, r, a, l, c] = [null, null, null, null, null];
  const p = Math.sqrt(i[0] * i[0] + i[1] * i[1]), f = i[0] / p, m = i[1] / p, v = A(s, t), P = A(t, e);
  return p > St ? Z(s, t, e) ? (a = h * v, l = t[0] - a * m, c = t[1] + a * f, o = [l, c], a = h * P, l = t[0] + a * m, c = t[1] - a * f, r = [l, c]) : (a = h * v, l = t[0] + a * m, c = t[1] - a * f, o = [l, c], a = h * P, l = t[0] - a * m, c = t[1] + a * f, r = [l, c]) : (l = t[0] + h * (s[0] - t[0]), c = t[1] + h * (s[1] - t[1]), o = [l, c], l = t[0] + h * (e[0] - t[0]), c = t[1] + h * (e[1] - t[1]), r = [l, c]), [o, r];
}, Et = (h, s, t) => {
  let e = h[0] - s[0], i = h[1] - s[1];
  const o = Math.sqrt(e * e + i * i);
  e /= o, i /= o;
  let r = t[0] - s[0], a = t[1] - s[1];
  const l = Math.sqrt(r * r + a * a);
  r /= l, a /= l;
  const c = e + r, p = i + a;
  return [c, p];
}, qi = (h, s) => {
  let [t, e, i, o, r] = [h[0], h[1], h[2], null, null];
  const l = Ct(0, t, e, i)[0], c = Et(t, e, i);
  if (Math.sqrt(c[0] * c[0] + c[1] * c[1]) > St) {
    const f = O(t, e), m = t[0] - f[0], v = t[1] - f[1], w = 2 / A(t, e), M = -w * v, S = w * m, C = M * M - S * S, F = 2 * M * S, G = S * S - M * M, D = l[0] - f[0], b = l[1] - f[1];
    o = f[0] + C * D + F * b, r = f[1] + F * D + G * b;
  } else
    o = t[0] + s * (e[0] - t[0]), r = t[1] + s * (e[1] - t[1]);
  return [o, r];
}, Xi = (h, s) => {
  const t = h.length, e = h[t - 3], i = h[t - 2], o = h[t - 1], a = Ct(0, e, i, o)[1], l = Et(e, i, o), c = Math.sqrt(l[0] * l[0] + l[1] * l[1]);
  let [p, f] = [null, null];
  if (c > St) {
    const m = O(i, o), v = o[0] - m[0], P = o[1] - m[1], M = 2 / A(i, o), S = -M * P, C = M * v, F = S * S - C * C, G = 2 * S * C, D = C * C - S * S, b = a[0] - m[0], $ = a[1] - m[1];
    p = m[0] + F * b + G * $, f = m[1] + G * b + D * $;
  } else
    p = o[0] + s * (i[0] - o[0]), f = o[1] + s * (i[1] - o[1]);
  return [p, f];
}, At = (h, s) => {
  const t = qi(s, h);
  let [e, i, o, r, a] = [null, null, null, [t], []];
  for (let c = 0; c < s.length - 2; c++) {
    [e, i, o] = [s[c], s[c + 1], s[c + 2]];
    const p = Ct(h, e, i, o);
    r = r.concat(p);
  }
  const l = Xi(s, h);
  l && r.push(l);
  for (let c = 0; c < s.length - 1; c++) {
    e = s[c], i = s[c + 1], a.push(e);
    for (let p = 0; p < ae; p++) {
      const f = Ni(p / ae, e, r[c * 2], r[c * 2 + 1], i);
      a.push(f);
    }
    a.push(i);
  }
  return a;
}, vt = function(h) {
  if (h.length <= 2)
    return h;
  const s = [], t = h.length - 1;
  for (let e = 0; e <= 1; e += 0.01) {
    let [i, o] = [0, 0];
    for (let r = 0; r <= t; r++) {
      const a = Yi(t, r), l = e ** r, c = (1 - e) ** (t - r);
      i += a * l * c * h[r][0], o += a * l * c * h[r][1];
    }
    s.push([i, o]);
  }
  return s.push(h[t]), s;
}, Tt = (h) => {
  let s = 1;
  switch (h) {
    case h <= 1:
      s = 1;
      break;
    case h === 2:
      s = 2;
      break;
    case h === 3:
      s = 6;
      break;
    case h === 24:
      s = 24;
      break;
    case h === 5:
      s = 120;
      break;
    default:
      for (let t = 1; t <= h; t++)
        s *= t;
      break;
  }
  return s;
}, Yi = (h, s) => Tt(h) / (Tt(s) * Tt(h - s)), B = (h) => {
  if (h.length <= 2)
    return h;
  const [s, t] = [2, []], e = h.length - s - 1;
  t.push(h[0]);
  for (let i = 0; i <= e; i++)
    for (let o = 0; o <= 1; o += 0.05) {
      let [r, a] = [0, 0];
      for (let l = 0; l <= s; l++) {
        const c = Ki(l, o);
        r += c * h[i + l][0], a += c * h[i + l][1];
      }
      t.push([r, a]);
    }
  return t.push(h[h.length - 1]), t;
}, Ki = (h, s) => {
  let t = 0;
  return h === 0 ? t = (s - 1) ** 2 / 2 : h === 1 ? t = (-2 * s ** 2 + 2 * s + 1) / 2 : h === 2 && (t = s ** 2 / 2), t;
};
class k {
  constructor(s, t, e) {
    g(this, "cesium");
    g(this, "viewer");
    g(this, "eventHandler");
    g(this, "polygonEntity");
    g(this, "geometryPoints", []);
    g(this, "state", "drawing");
    g(this, "controlPoints", []);
    g(this, "controlPointsEventHandler");
    g(this, "lineEntity");
    g(this, "type");
    g(this, "freehand");
    g(this, "style");
    g(this, "outlineEntity");
    g(this, "eventDispatcher");
    g(this, "dragEventHandler");
    g(this, "entityId", "");
    g(this, "points", []);
    g(this, "styleCache");
    g(this, "minPointsForShape", 0);
    g(this, "tempLineEntity");
    this.cesium = s, this.viewer = t, this.type = this.getType(), this.mergeStyle(e), this.cartesianToLnglat = this.cartesianToLnglat.bind(this), this.pixelToCartesian = this.pixelToCartesian.bind(this), this.eventDispatcher = new Hi(), t.trackedEntity = void 0, t.cesiumWidget.screenSpaceEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_DOUBLE_CLICK), this.onClick();
  }
  mergeStyle(s) {
    this.type === "polygon" ? this.style = Object.assign(
      {
        material: new this.cesium.Color(),
        outlineMaterial: new this.cesium.Color(),
        outlineWidth: 2
      },
      s
    ) : this.type === "line" && (this.style = Object.assign(
      {
        material: new this.cesium.Color(),
        lineWidth: 2
      },
      s
    )), this.styleCache = Bi(this.style);
  }
  /**
   * The base class provides a method to change the state, and different logic is implemented based on the state.
   *  The state is controlled by individual sub-components according to the actual situation.
   * @param state
   */
  setState(s) {
    this.state = s;
  }
  getState() {
    return this.state;
  }
  /**
   * Bind a global click event that responds differently based on the state. When in the drawing state,
   * a click will add points for geometric shapes. During editing, selecting a drawn shape puts it in an
   *  editable state. Clicking on empty space sets it to a static state.
   */
  onClick() {
    this.eventHandler = new this.cesium.ScreenSpaceEventHandler(this.viewer.canvas), this.eventHandler.setInputAction((s) => {
      const t = this.viewer.scene.pick(s.position), e = this.cesium.defined(t) && t.id instanceof this.cesium.Entity;
      let i = this.polygonEntity;
      if (this.type === "line" && (i = this.lineEntity), this.state === "drawing") {
        const o = this.pixelToCartesian(s.position), r = this.getPoints();
        if (!o || !this.freehand && r.length > 0 && !this.checkDistance(o, r[r.length - 1]))
          return;
        this.addPoint(o), this.getPoints().length === 1 && this.eventDispatcher.dispatchEvent("drawStart"), this.eventDispatcher.dispatchEvent("drawUpdate", o);
      } else if (this.state === "edit")
        (!e || i.id !== t.id.id) && (this.setState("static"), this.removeControlPoints(), this.disableDrag(), this.eventDispatcher.dispatchEvent("editEnd", this.getPoints()));
      else if (this.state === "static" && e && i.id === t.id.id) {
        const o = this.type === "line" ? t.id.polyline : t.id.polygon;
        this.cesium.defined(o) && (this.setState("edit"), this.addControlPoints(), this.draggable(), this.eventDispatcher.dispatchEvent("editStart"));
      }
    }, this.cesium.ScreenSpaceEventType.LEFT_CLICK);
  }
  onMouseMove() {
    this.eventHandler.setInputAction((s) => {
      const t = this.getPoints(), e = this.pixelToCartesian(s.endPosition);
      e && this.checkDistance(e, t[t.length - 1]) && this.updateMovingPoint(e, t.length);
    }, this.cesium.ScreenSpaceEventType.MOUSE_MOVE);
  }
  onDoubleClick() {
    this.eventHandler.setInputAction((s) => {
      this.state === "drawing" && this.finishDrawing();
    }, this.cesium.ScreenSpaceEventType.LEFT_DOUBLE_CLICK);
  }
  /**
   * Check if the distance between two points is greater than 10 meters.
   */
  checkDistance(s, t) {
    return this.cesium.Cartesian3.distance(s, t) > 10;
  }
  finishDrawing() {
    this.type === "polygon" && this.lineEntity && this.viewer.entities.remove(this.lineEntity), this.removeMoveListener(), this.setState("edit"), this.addControlPoints(), this.draggable();
    const s = this.polygonEntity || this.lineEntity;
    this.entityId = s.id, this.eventDispatcher.dispatchEvent("drawEnd", this.getPoints());
  }
  removeClickListener() {
    this.eventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_CLICK);
  }
  removeMoveListener() {
    this.eventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.MOUSE_MOVE);
  }
  removeDoubleClickListener() {
    this.eventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_DOUBLE_CLICK);
  }
  setGeometryPoints(s) {
    this.geometryPoints = s;
  }
  getGeometryPoints() {
    return this.geometryPoints;
  }
  drawPolygon() {
    const s = () => new this.cesium.PolygonHierarchy(this.geometryPoints);
    if (!this.polygonEntity) {
      const t = this.style;
      this.polygonEntity = this.viewer.entities.add({
        polygon: new this.cesium.PolygonGraphics({
          hierarchy: new this.cesium.CallbackProperty(s, !1),
          show: !0,
          material: t.material
        })
      }), this.outlineEntity = this.viewer.entities.add({
        polyline: {
          positions: new this.cesium.CallbackProperty(() => [...this.geometryPoints, this.geometryPoints[0]], !1),
          width: t.outlineWidth,
          material: t.outlineMaterial,
          clampToGround: !0
        }
      });
    }
  }
  drawLine() {
    if (!this.lineEntity) {
      const s = this.style;
      this.lineEntity = this.addLineEntity(s);
    }
  }
  addTempLine() {
    if (!this.tempLineEntity) {
      const s = this.style, t = {
        material: s.outlineMaterial,
        lineWidth: s.outlineWidth
      };
      this.tempLineEntity = this.addLineEntity(t);
    }
  }
  removeTempLine() {
    this.tempLineEntity && this.viewer.entities.remove(this.tempLineEntity);
  }
  addLineEntity(s) {
    return this.viewer.entities.add({
      polyline: {
        positions: new this.cesium.CallbackProperty(() => this.geometryPoints, !1),
        width: s.lineWidth,
        material: s.material,
        clampToGround: !0
      }
    });
  }
  cartesianToLnglat(s) {
    const t = this.viewer.scene.globe.ellipsoid.cartesianToCartographic(s), e = this.cesium.Math.toDegrees(t.latitude);
    return [this.cesium.Math.toDegrees(t.longitude), e];
  }
  pixelToCartesian(s) {
    const t = this.viewer.camera.getPickRay(s);
    return this.viewer.scene.globe.pick(t, this.viewer.scene);
  }
  /**
   * Display key points when creating a shape, allowing dragging of these points to edit and generate new shapes.
   */
  addControlPoints() {
    const s = this.getPoints();
    this.controlPoints = s.map((o) => this.viewer.entities.add({
      position: o,
      point: {
        pixelSize: 10,
        heightReference: this.cesium.HeightReference.CLAMP_TO_GROUND,
        color: this.cesium.Color.RED
      }
    }));
    let t = !1, e = null, i;
    this.controlPointsEventHandler = new this.cesium.ScreenSpaceEventHandler(this.viewer.canvas), this.controlPointsEventHandler.setInputAction((o) => {
      const r = this.viewer.scene.pick(o.position);
      if (this.cesium.defined(r)) {
        for (let a = 0; a < this.controlPoints.length; a++)
          if (r.id === this.controlPoints[a]) {
            t = !0, e = this.controlPoints[a], i = e.position._value, e.index = a;
            break;
          }
        this.viewer.scene.screenSpaceCameraController.enableRotate = !1;
      }
    }, this.cesium.ScreenSpaceEventType.LEFT_DOWN), this.controlPointsEventHandler.setInputAction((o) => {
      if (t && e) {
        const r = this.viewer.camera.pickEllipsoid(o.endPosition, this.viewer.scene.globe.ellipsoid);
        r && (e.position.setValue(r), this.updateDraggingPoint(r, e.index));
      }
    }, this.cesium.ScreenSpaceEventType.MOUSE_MOVE), this.controlPointsEventHandler.setInputAction(() => {
      e && !this.cesium.Cartesian3.equals(i, e.position._value) && this.eventDispatcher.dispatchEvent("drawUpdate", e.position._value), t = !1, e = null, this.viewer.scene.screenSpaceCameraController.enableRotate = !0;
    }, this.cesium.ScreenSpaceEventType.LEFT_UP);
  }
  removeControlPoints() {
    this.controlPoints.length > 0 && (this.controlPoints.forEach((s) => {
      this.viewer.entities.remove(s);
    }), this.controlPointsEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_DOWN), this.controlPointsEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.MOUSE_MOVE), this.controlPointsEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_UP));
  }
  /**
   * Allow the entire shape to be dragged while in edit mode.
   */
  draggable() {
    let s = !1, t;
    this.dragEventHandler = new this.cesium.ScreenSpaceEventHandler(this.viewer.canvas), this.dragEventHandler.setInputAction((e) => {
      const i = this.viewer.scene.camera.getPickRay(e.position);
      if (i) {
        const o = this.viewer.scene.globe.pick(i, this.viewer.scene), r = this.viewer.scene.pick(e.position);
        if (this.cesium.defined(r) && r.id instanceof this.cesium.Entity) {
          const a = r.id;
          this.isCurrentEntity(a.id) && (s = !0, t = o, this.viewer.scene.screenSpaceCameraController.enableRotate = !1);
        }
      }
    }, this.cesium.ScreenSpaceEventType.LEFT_DOWN), this.dragEventHandler.setInputAction((e) => {
      if (s && t) {
        const i = this.pixelToCartesian(e.endPosition);
        if (i) {
          const o = this.cesium.Cartesian3.subtract(i, t, new this.cesium.Cartesian3()), r = this.geometryPoints.map((a) => this.cesium.Cartesian3.add(a, o, new this.cesium.Cartesian3()));
          this.points = this.points.map((a) => this.cesium.Cartesian3.add(a, o, new this.cesium.Cartesian3())), this.controlPoints.map((a) => {
            var p, f;
            const l = (p = a.position) == null ? void 0 : p.getValue(this.cesium.JulianDate.now()), c = this.cesium.Cartesian3.add(l, o, new this.cesium.Cartesian3());
            (f = a.position) == null || f.setValue(c);
          }), this.setGeometryPoints(r), this.minPointsForShape === 4 && (this.curveControlPointLeft = this.cesium.Cartesian3.add(this.curveControlPointLeft, o, new this.cesium.Cartesian3()), this.curveControlPointRight = this.cesium.Cartesian3.add(this.curveControlPointRight, o, new this.cesium.Cartesian3())), t = i;
        }
      } else if (this.viewer.scene.camera.getPickRay(e.endPosition)) {
        const o = this.viewer.scene.pick(e.endPosition);
        if (this.cesium.defined(o) && o.id instanceof this.cesium.Entity) {
          const r = o.id;
          this.isCurrentEntity(r.id) ? this.viewer.scene.canvas.style.cursor = "move" : this.viewer.scene.canvas.style.cursor = "default";
        } else
          this.viewer.scene.canvas.style.cursor = "default";
      }
    }, this.cesium.ScreenSpaceEventType.MOUSE_MOVE), this.dragEventHandler.setInputAction(() => {
      s = !1, t = void 0, this.viewer.scene.screenSpaceCameraController.enableRotate = !0;
    }, this.cesium.ScreenSpaceEventType.LEFT_UP);
  }
  // Finish editing, disable dragging."
  disableDrag() {
    this.dragEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_DOWN), this.dragEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.MOUSE_MOVE), this.dragEventHandler.removeInputAction(this.cesium.ScreenSpaceEventType.LEFT_UP);
  }
  show(s) {
    if (s) {
      const { duration: t, delay: e, callback: i } = s;
      this.showWithAnimation(t, e, i);
      return;
    } else
      this.showWithAnimation(0, 0);
  }
  hide(s) {
    if (s) {
      const { duration: t, delay: e, callback: i } = s;
      this.hideWithAnimation(t, e, i);
      return;
    } else
      this.hideWithAnimation(0, 0);
  }
  showWithAnimation(s = 2e3, t = 0, e) {
    var i, o, r, a;
    if (this.state === "hidden") {
      if (this.setState("static"), this.type === "polygon") {
        let l = 0.3;
        const c = this.styleCache.material;
        c.image ? l = c.color.getValue().alpha : l = c.alpha, this.animateOpacity(this.polygonEntity, l, s, t, e, this.state);
        const p = (o = (i = this.styleCache) == null ? void 0 : i.outlineMaterial) == null ? void 0 : o.alpha;
        this.animateOpacity(this.outlineEntity, p || 1, s, t, void 0, this.state);
      } else if (this.type === "line") {
        const l = this.styleCache.material;
        let c = 1;
        l.image ? c = l.color.alpha : l.dashLength ? c = l.color.getValue().alpha : c = (a = (r = this.styleCache) == null ? void 0 : r.material) == null ? void 0 : a.alpha, this.animateOpacity(this.lineEntity, c, s, t, e, this.state);
      }
      s != 0 && this.setState("animating");
    }
  }
  hideWithAnimation(s = 2e3, t = 0, e) {
    this.state === "hidden" || this.state != "static" || (this.setState("hidden"), this.type === "polygon" ? (this.animateOpacity(this.polygonEntity, 0, s, t, e, this.state), this.animateOpacity(this.outlineEntity, 0, s, t, void 0, this.state)) : this.type === "line" && this.animateOpacity(this.lineEntity, 0, s, t, e, this.state), s != 0 && this.setState("animating"));
  }
  animateOpacity(s, t, e, i, o, r) {
    setTimeout(() => {
      const a = s.polygon || s.polyline || s.billboard;
      let l, c = a.material;
      c ? c.image && c.color.alpha !== void 0 ? l = c.color.alpha : l = c.color.getValue().alpha : l = a.color.getValue().alpha;
      let p = 0;
      const f = (m) => {
        p || (p = m);
        const v = m - p;
        if (v < e) {
          const P = v / e * (t - l), w = l + P;
          if (c)
            if (c.image && c.color.alpha !== void 0)
              c.color.alpha = w;
            else {
              const M = c.color.getValue().withAlpha(w);
              c.color.setValue(M);
            }
          else {
            const S = a.color.getValue().withAlpha(w);
            a.color.setValue(S);
          }
          requestAnimationFrame(f);
        } else {
          o && o();
          const P = r || "static";
          if (c)
            if (c.image && c.color.alpha !== void 0)
              c.color.alpha = t;
            else {
              const w = c.color.getValue().withAlpha(t);
              c.color.setValue(w);
            }
          else {
            const M = a.color.getValue().withAlpha(t);
            a.color.setValue(M);
          }
          requestAnimationFrame(() => {
            this.setState(P);
          });
        }
      };
      requestAnimationFrame(f);
    }, i);
  }
  startGrowthAnimation(s) {
    const { duration: t = 2e3, delay: e = 0, callback: i } = s || {};
    if (!(this.state === "hidden" || this.state != "static")) {
      if (!this.minPointsForShape) {
        console.warn("Growth animation is not supported for this type of shape");
        return;
      }
      if (this.setState("animating"), this.minPointsForShape === 4) {
        this.doubleArrowGrowthAnimation(t, e, i);
        return;
      }
      setTimeout(() => {
        this.hideWithAnimation(0, 0, void 0);
        const o = this.getPoints();
        let r = 0;
        this.minPointsForShape === 2 ? r = t / (o.length - 1) : r = t / (o.length - 2);
        let a = Date.now(), l = 0;
        this.viewer.clock.shouldAnimate = !0;
        const c = (p) => {
          const m = Date.now() - a;
          if (m >= t) {
            i && i(), a = 0, this.viewer.clock.shouldAnimate = !1, this.viewer.clock.onTick.removeEventListener(c), this.setState("static");
            return;
          }
          const v = Math.floor(m / r);
          let P;
          this.minPointsForShape === 2 ? l = v + 1 : l = v + 2, P = o[l - 1], v == 0 && this.minPointsForShape === 3 && (P = this.cesium.Cartesian3.midpoint(o[0], o[1], new this.cesium.Cartesian3()));
          let w = o[l];
          const M = (m - v * r) / r, S = this.cesium.Cartesian3.lerp(P, w, M, new this.cesium.Cartesian3()), C = o.slice(0, l + 1);
          C[C.length - 1] = S;
          const F = this.createGraphic(C);
          this.setGeometryPoints(F), this.showWithAnimation(0, 0, void 0);
        };
        this.viewer.clock.onTick.addEventListener(c);
      }, e);
    }
  }
  doubleArrowGrowthAnimation(s = 2e3, t = 0, e) {
    setTimeout(() => {
      this.hideWithAnimation(0, 0, void 0);
      const i = this.getPoints();
      let o = Date.now();
      this.viewer.clock.shouldAnimate = !0;
      const r = (a) => {
        const c = Date.now() - o;
        if (c >= s) {
          e && e(), o = 0, this.viewer.clock.shouldAnimate = !1, this.viewer.clock.onTick.removeEventListener(r), this.setState("static");
          return;
        }
        const p = this.cesium.Cartesian3.midpoint(i[0], i[1], new this.cesium.Cartesian3()), f = this.cesium.Cartesian3.midpoint(i[0], p, new this.cesium.Cartesian3()), m = this.cesium.Cartesian3.midpoint(p, i[1], new this.cesium.Cartesian3());
        let v = i[3], P = i[2];
        const w = c / s, M = this.getBezierControlPointforGrowthAnimation();
        let S = [f, M.left, v], C = [m, M.right, P];
        const F = this.getNewPosition(S, w), G = this.getNewPosition(C, w), D = [...i];
        D[2] = G, D[3] = F;
        const b = this.createGraphic(D);
        this.setGeometryPoints(b), this.showWithAnimation(0, 0, void 0);
      };
      this.viewer.clock.onTick.addEventListener(r);
    }, t);
  }
  getNewPosition(s, t) {
    s = s.map((o) => this.cartesianToLnglat(o));
    let e = At(0.3, s);
    return e = e.map((o) => this.cesium.Cartesian3.fromDegrees(o[0], o[1])), this.interpolateAlongCurve(e, t);
  }
  interpolateAlongCurve(s, t) {
    const e = s.length - 1, i = Math.floor(t * e), o = t * e - i, r = s[i], a = s[i + 1], l = r.x + (a.x - r.x) * o, c = r.y + (a.y - r.y) * o, p = r.z + (a.z - r.z) * o;
    return new this.cesium.Cartesian3(l, c, p);
  }
  remove() {
    this.type === "polygon" ? (this.viewer.entities.remove(this.polygonEntity), this.viewer.entities.remove(this.outlineEntity), this.polygonEntity = null, this.outlineEntity = null, this.lineEntity = null) : this.type === "line" && this.viewer.entities.remove(this.lineEntity), this.removeClickListener(), this.removeMoveListener(), this.removeDoubleClickListener(), this.removeControlPoints();
  }
  on(s, t) {
    this.eventDispatcher.on(s, t);
  }
  off(s, t) {
    this.eventDispatcher.off(s, t);
  }
  isCurrentEntity(s) {
    return this.entityId === s;
  }
  addPoint(s) {
  }
  getPoints() {
    return [new this.cesium.Cartesian3()];
  }
  updateMovingPoint(s, t) {
  }
  updateDraggingPoint(s, t) {
  }
  getType() {
    return "polygon";
  }
  createGraphic(s) {
    return [new this.cesium.Cartesian3()];
  }
}
class le extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "arrowLengthScale", 5);
    g(this, "maxArrowLength", 2);
    g(this, "tailWidthFactor");
    g(this, "neckWidthFactor");
    g(this, "headWidthFactor");
    g(this, "headAngle");
    g(this, "neckAngle");
    g(this, "minPointsForShape");
    this.cesium = t, this.tailWidthFactor = 0.1, this.neckWidthFactor = 0.2, this.headWidthFactor = 0.25, this.headAngle = Math.PI / 8.5, this.neckAngle = Math.PI / 13, this.minPointsForShape = 2, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    if (this.points.length < 2 && (this.points.push(t), this.onMouseMove()), this.points.length === 2) {
      const e = this.createGraphic(this.points);
      this.setGeometryPoints(e), this.drawPolygon(), this.finishDrawing();
    }
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t], i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const [e, i] = t.map(this.cartesianToLnglat), o = x([e, i]), r = o * this.tailWidthFactor, a = o * this.neckWidthFactor, l = o * this.headWidthFactor, c = T(i, e, Math.PI / 2, r, !0), p = T(i, e, Math.PI / 2, r, !1), f = T(e, i, this.headAngle, l, !1), m = T(e, i, this.headAngle, l, !0), v = T(e, i, this.neckAngle, a, !1), P = T(e, i, this.neckAngle, a, !0), w = [...c, ...v, ...f, ...i, ...m, ...P, ...p, ...e];
    return this.cesium.Cartesian3.fromDegreesArray(w);
  }
  getPoints() {
    return this.points;
  }
}
class Lt extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "headHeightFactor");
    g(this, "headWidthFactor");
    g(this, "neckHeightFactor");
    g(this, "neckWidthFactor");
    g(this, "headTailFactor");
    g(this, "minPointsForShape");
    this.cesium = t, this.headHeightFactor = 0.18, this.headWidthFactor = 0.3, this.neckHeightFactor = 0.85, this.neckWidthFactor = 0.15, this.headTailFactor = 0.8, this.minPointsForShape = 3, this.setState("drawing"), this.onDoubleClick();
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length < 2 ? this.onMouseMove() : this.points.length === 2 && (this.setGeometryPoints(this.points), this.drawPolygon());
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    if (this.setGeometryPoints(e), e.length === 2)
      this.addTempLine();
    else {
      this.removeTempLine();
      const i = this.createGraphic(e);
      this.setGeometryPoints(i), this.drawPolygon();
    }
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const e = t.map((F) => this.cartesianToLnglat(F));
    let [i, o] = [e[0], e[1]];
    Z(e[0], e[1], e[2]) && (i = e[1], o = e[0]);
    const a = [O(i, o)].concat(e.slice(2)), l = this.getArrowHeadPoints(a, i, o), [c, p] = [l[0], l[4]], f = A(i, o) / x(a), m = this.getArrowBodyPoints(a, c, p, f), v = m.length;
    let P = [i].concat(m.slice(0, v / 2));
    P.push(c);
    let w = [o].concat(m.slice(v / 2, v));
    w.push(p), P = B(P), w = B(w);
    const M = P.concat(l, w.reverse()), S = [].concat(...M);
    return this.cesium.Cartesian3.fromDegreesArray(S);
  }
  getPoints() {
    return this.points;
  }
  getArrowHeadPoints(t, e, i) {
    try {
      let o = x(t), r = o * this.headHeightFactor;
      const a = t[t.length - 1];
      o = A(a, t[t.length - 2]);
      const l = A(e, i);
      r > l * this.headTailFactor && (r = l * this.headTailFactor);
      const c = r * this.headWidthFactor, p = r * this.neckWidthFactor;
      r = r > o ? o : r;
      const f = r * this.neckHeightFactor, m = T(t[t.length - 2], a, 0, r, !0), v = T(t[t.length - 2], a, 0, f, !0), P = T(a, m, Math.PI / 2, c, !1), w = T(a, m, Math.PI / 2, c, !0), M = T(a, v, Math.PI / 2, p, !1), S = T(a, v, Math.PI / 2, p, !0);
      return [M, P, a, w, S];
    } catch (o) {
      console.log(o);
    }
  }
  getArrowBodyPoints(t, e, i, o) {
    const r = ht(t), l = x(t) * o, c = A(e, i), p = (l - c) / 2;
    let [f, m, v] = [0, [], []];
    for (let P = 1; P < t.length - 1; P++) {
      const w = Mt(t[P - 1], t[P], t[P + 1]) / 2;
      f += A(t[P - 1], t[P]);
      const M = (l / 2 - f / r * p) / Math.sin(w), S = T(t[P - 1], t[P], Math.PI - w, M, !0), C = T(t[P - 1], t[P], w, M, !1);
      m.push(S), v.push(C);
    }
    return m.concat(v);
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
}
class zi extends Lt {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "headHeightFactor");
    g(this, "headWidthFactor");
    g(this, "neckHeightFactor");
    g(this, "neckWidthFactor");
    g(this, "headTailFactor");
    g(this, "tailWidthFactor");
    g(this, "swallowTailFactor");
    g(this, "swallowTailPnt");
    this.cesium = t, this.headHeightFactor = 0.18, this.headWidthFactor = 0.3, this.neckHeightFactor = 0.85, this.neckWidthFactor = 0.15, this.tailWidthFactor = 0.1, this.headTailFactor = 0.8, this.swallowTailFactor = 1, this.swallowTailPnt = [0, 0], this.minPointsForShape = 3;
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const e = t.map((b) => this.cartesianToLnglat(b));
    let [i, o] = [e[0], e[1]];
    Z(e[0], e[1], e[2]) && (i = e[1], o = e[0]);
    const a = [O(i, o)].concat(e.slice(2)), l = this.getArrowHeadPoints(a, i, o), [c, p] = [l[0], l[4]], f = A(i, o), m = x(a), v = m * this.tailWidthFactor * this.swallowTailFactor;
    this.swallowTailPnt = T(a[1], a[0], 0, v, !0);
    const P = f / m, w = this.getArrowBodyPoints(a, c, p, P), M = w.length;
    let S = [i].concat(w.slice(0, M / 2));
    S.push(c);
    let C = [o].concat(w.slice(M / 2, M));
    C.push(p), S = B(S), C = B(C);
    const F = S.concat(l, C.reverse(), [this.swallowTailPnt, S[0]]), G = [].concat(...F);
    return this.cesium.Cartesian3.fromDegreesArray(G);
  }
}
class ue extends Lt {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "headHeightFactor");
    g(this, "headWidthFactor");
    g(this, "neckHeightFactor");
    g(this, "neckWidthFactor");
    g(this, "tailWidthFactor");
    this.cesium = t, this.headHeightFactor = 0.18, this.headWidthFactor = 0.3, this.neckHeightFactor = 0.85, this.neckWidthFactor = 0.15, this.tailWidthFactor = 0.1, this.minPointsForShape = 2;
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length < 2 ? this.onMouseMove() : this.points.length > 2 && this.lineEntity && this.viewer.entities.remove(this.lineEntity);
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    if (this.setGeometryPoints(e), !(e.length < 2)) {
      const i = this.createGraphic(e);
      this.setGeometryPoints(i), this.drawPolygon();
    }
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const e = t.map((w) => this.cartesianToLnglat(w)), i = this.getTailPoints(e), o = this.getArrowHeadPoints(e, i[0], i[1]), r = o[0], a = o[4], l = this.getArrowBodyPoints(e, r, a, this.tailWidthFactor), c = l.length;
    let p = [i[0]].concat(l.slice(0, c / 2));
    p.push(r);
    let f = [i[1]].concat(l.slice(c / 2, c));
    f.push(a), p = B(p), f = B(f);
    const m = p.concat(o, f.reverse()), v = [].concat(...m);
    return this.cesium.Cartesian3.fromDegreesArray(v);
  }
  getTailPoints(t) {
    const i = x(t) * this.tailWidthFactor, o = T(t[1], t[0], Math.PI / 2, i, !1), r = T(t[1], t[0], Math.PI / 2, i, !0);
    return [o, r];
  }
}
class $i extends ue {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "headHeightFactor");
    g(this, "headWidthFactor");
    g(this, "neckHeightFactor");
    g(this, "neckWidthFactor");
    g(this, "tailWidthFactor");
    g(this, "swallowTailFactor");
    this.cesium = t, this.headHeightFactor = 0.18, this.headWidthFactor = 0.3, this.neckHeightFactor = 0.85, this.neckWidthFactor = 0.15, this.tailWidthFactor = 0.1, this.swallowTailFactor = 1, this.minPointsForShape = 2;
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const e = t.map((w) => this.cartesianToLnglat(w)), i = this.getTailPoints(e), o = this.getArrowHeadPoints(e, i[0], i[2]), r = o[0], a = o[4], l = this.getArrowBodyPoints(e, r, a, this.tailWidthFactor), c = l.length;
    let p = [i[0]].concat(l.slice(0, c / 2));
    p.push(r);
    let f = [i[2]].concat(l.slice(c / 2, c));
    f.push(a), p = B(p), f = B(f);
    const m = p.concat(o, f.reverse(), [i[1], p[0]]), v = [].concat(...m);
    return this.cesium.Cartesian3.fromDegreesArray(v);
  }
  getTailPoints(t) {
    const i = x(t) * this.tailWidthFactor, o = T(t[1], t[0], Math.PI / 2, i, !1), r = T(t[1], t[0], Math.PI / 2, i, !0), a = i * this.swallowTailFactor, l = T(t[1], t[0], 0, a, !0);
    return [o, l, r];
  }
}
class Ji extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "arrowLengthScale", 5);
    g(this, "maxArrowLength", 3e6);
    g(this, "minPointsForShape");
    this.cesium = t, this.minPointsForShape = 2, this.setState("drawing");
  }
  getType() {
    return "line";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    if (this.points.length < 2 && (this.points.push(t), this.onMouseMove()), this.points.length === 2) {
      const e = this.createGraphic(this.points);
      this.setGeometryPoints(e), this.drawLine(), this.finishDrawing();
    }
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t], i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawLine();
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawLine();
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const [e, i] = t.map(this.cartesianToLnglat);
    let r = A(e, i) / this.arrowLengthScale;
    r = r > this.maxArrowLength ? this.maxArrowLength : r;
    const a = T(e, i, Math.PI / 6, r / 2, !1), l = T(e, i, Math.PI / 6, r / 2, !0), c = [...e, ...i, ...a, ...i, ...l];
    return this.cesium.Cartesian3.fromDegreesArray(c);
  }
  getPoints() {
    return this.points;
  }
}
class Qi extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "arrowLengthScale", 5);
    g(this, "maxArrowLength", 3e6);
    g(this, "t");
    g(this, "minPointsForShape");
    this.cesium = t, this.t = 0.3, this.minPointsForShape = 2, this.setState("drawing"), this.onDoubleClick();
  }
  getType() {
    return "line";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length < 2 && this.onMouseMove();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    let i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawLine();
  }
  createStraightArrow(t) {
    const [e, i] = t.map(this.cartesianToLnglat);
    let r = A(e, i) / this.arrowLengthScale;
    r = r > this.maxArrowLength ? this.maxArrowLength : r;
    const a = T(e, i, Math.PI / 6, r / 2, !1), l = T(e, i, Math.PI / 6, r / 2, !0), c = [...e, ...i, ...a, ...i, ...l];
    return this.cesium.Cartesian3.fromDegreesArray(c);
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawLine();
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const e = t.map((P) => this.cartesianToLnglat(P));
    if (t.length === 2)
      return this.createStraightArrow(t);
    const i = At(this.t, e), o = e[e.length - 2], r = e[e.length - 1];
    let l = ht(e) / this.arrowLengthScale;
    l = l > this.maxArrowLength ? this.maxArrowLength : l;
    const c = T(o, r, Math.PI / 6, l / 2, !1), p = T(o, r, Math.PI / 6, l / 2, !0), m = [...[].concat(...i), ...c, ...r, ...p];
    return this.cesium.Cartesian3.fromDegreesArray(m);
  }
  getPoints() {
    return this.points;
  }
}
class Zi extends le {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "arrowLengthScale", 5);
    g(this, "maxArrowLength", 2);
    g(this, "tailWidthFactor");
    g(this, "neckWidthFactor");
    g(this, "headWidthFactor");
    g(this, "headAngle");
    g(this, "neckAngle");
    g(this, "minPointsForShape");
    this.cesium = t, this.tailWidthFactor = 0.08, this.neckWidthFactor = 0.1, this.headWidthFactor = 0.13, this.headAngle = Math.PI / 4, this.neckAngle = Math.PI * 0.17741, this.minPointsForShape = 2, this.setState("drawing");
  }
  createGraphic(t) {
    const [e, i] = t.map(this.cartesianToLnglat), o = x([e, i]) * 1.5, r = o * this.tailWidthFactor, a = o * this.neckWidthFactor, l = o * this.headWidthFactor, c = T(i, e, Math.PI / 2, r, !0), p = T(i, e, Math.PI / 2, r, !1), f = T(e, i, this.headAngle, l, !1), m = T(e, i, this.headAngle, l, !0), v = T(e, i, this.neckAngle, a, !1), P = T(e, i, this.neckAngle, a, !0), w = [...c, ...v, ...f, ...i, ...m, ...P, ...p, ...e];
    return this.cesium.Cartesian3.fromDegreesArray(w);
  }
}
class tn extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "arrowLengthScale", 5);
    g(this, "maxArrowLength", 2);
    g(this, "neckWidthFactor");
    g(this, "headWidthFactor");
    g(this, "headHeightFactor");
    g(this, "neckHeightFactor");
    g(this, "connPoint");
    g(this, "tempPoint4");
    g(this, "minPointsForShape");
    g(this, "llBodyPnts", []);
    g(this, "rrBodyPnts", []);
    g(this, "curveControlPointLeft");
    g(this, "curveControlPointRight");
    g(this, "isClockWise");
    this.cesium = t, this.headHeightFactor = 0.25, this.headWidthFactor = 0.3, this.neckHeightFactor = 0.85, this.neckWidthFactor = 0.15, this.connPoint = [0, 0], this.tempPoint4 = [0, 0], this.minPointsForShape = 4, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length < 2 ? this.onMouseMove() : this.points.length === 2 ? (this.setGeometryPoints(this.points), this.drawPolygon()) : this.points.length === 3 ? this.lineEntity && this.viewer.entities.remove(this.lineEntity) : this.finishDrawing();
  }
  finishDrawing() {
    this.curveControlPointLeft = this.cesium.Cartesian3.fromDegrees(this.llBodyPnts[2][0], this.llBodyPnts[2][1]), this.curveControlPointRight = this.cesium.Cartesian3.fromDegrees(this.rrBodyPnts[1][0], this.rrBodyPnts[1][1]), super.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    if (this.setGeometryPoints(e), e.length === 2)
      this.addTempLine();
    else if (e.length > 2) {
      this.removeTempLine();
      const i = this.createGraphic(e);
      this.setGeometryPoints(i), this.drawPolygon();
    }
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * Generate geometric shapes based on key points.
   */
  createGraphic(t) {
    const e = t.map((b) => this.cartesianToLnglat(b)), [i, o, r] = [e[0], e[1], e[2]], a = e.length;
    a === 3 ? (this.tempPoint4 = this.getTempPoint4(i, o, r), this.connPoint = O(i, o)) : a === 4 ? (this.tempPoint4 = e[3], this.connPoint = O(i, o)) : (this.tempPoint4 = e[3], this.connPoint = e[4]);
    let l, c;
    this.isClockWise = Z(i, o, r), this.isClockWise ? (l = this.getArrowPoints(i, this.connPoint, this.tempPoint4, !1), c = this.getArrowPoints(this.connPoint, o, r, !0)) : (l = this.getArrowPoints(o, this.connPoint, r, !1), c = this.getArrowPoints(this.connPoint, i, this.tempPoint4, !0));
    const p = l.length, f = (p - 5) / 2, m = l.slice(0, f), v = l.slice(f, f + 5);
    let P = l.slice(f + 5, p);
    this.llBodyPnts = m;
    let w = c.slice(0, f);
    const M = c.slice(f, f + 5), S = c.slice(f + 5, p);
    this.rrBodyPnts = S, w = vt(w);
    const C = vt(S.concat(m.slice(1)));
    P = vt(P);
    const F = w.concat(M, C, v, P), G = [].concat(...F);
    return this.cesium.Cartesian3.fromDegreesArray(G);
  }
  getTempPoint4(t, e, i) {
    const o = O(t, e), r = A(o, i), a = Mt(t, o, i);
    let l = [0, 0], c, p, f;
    return a < Math.PI / 2 ? (c = r * Math.sin(a), p = r * Math.cos(a), f = T(t, o, Math.PI / 2, c, !1), l = T(o, f, Math.PI / 2, p, !0)) : a >= Math.PI / 2 && a < Math.PI ? (c = r * Math.sin(Math.PI - a), p = r * Math.cos(Math.PI - a), f = T(t, o, Math.PI / 2, c, !1), l = T(o, f, Math.PI / 2, p, !1)) : a >= Math.PI && a < Math.PI * 1.5 ? (c = r * Math.sin(a - Math.PI), p = r * Math.cos(a - Math.PI), f = T(t, o, Math.PI / 2, c, !0), l = T(o, f, Math.PI / 2, p, !0)) : (c = r * Math.sin(Math.PI * 2 - a), p = r * Math.cos(Math.PI * 2 - a), f = T(t, o, Math.PI / 2, c, !0), l = T(o, f, Math.PI / 2, p, !1)), l;
  }
  getArrowPoints(t, e, i, o) {
    const r = O(t, e), a = A(r, i);
    let l = T(i, r, 0, a * 0.3, !0), c = T(i, r, 0, a * 0.5, !0);
    l = T(r, l, Math.PI / 2, a / 5, o), c = T(r, c, Math.PI / 2, a / 4, o);
    const p = [r, l, c, i], f = this.getArrowHeadPoints(p);
    if (f && Array.isArray(f) && f.length > 0) {
      const m = f[0], v = f[4], P = A(t, e) / x(p) / 2, w = this.getArrowBodyPoints(p, m, v, P);
      if (w) {
        const M = w.length;
        let S = w.slice(0, M / 2), C = w.slice(M / 2, M);
        return S.push(m), C.push(v), S = S.reverse(), S.push(e), C = C.reverse(), C.push(t), S.reverse().concat(f, C);
      }
    } else
      throw new Error("Interpolation Error");
  }
  getArrowBodyPoints(t, e, i, o) {
    const r = ht(t), l = x(t) * o, c = A(e, i), p = (l - c) / 2;
    let f = 0, m = [], v = [];
    for (let P = 1; P < t.length - 1; P++) {
      const w = Mt(t[P - 1], t[P], t[P + 1]) / 2;
      f += A(t[P - 1], t[P]);
      const M = (l / 2 - f / r * p) / Math.sin(w), S = T(t[P - 1], t[P], Math.PI - w, M, !0), C = T(t[P - 1], t[P], w, M, !1);
      m.push(S), v.push(C);
    }
    return m.concat(v);
  }
  getArrowHeadPoints(t) {
    const i = x(t) * this.headHeightFactor, o = t[t.length - 1], r = i * this.headWidthFactor, a = i * this.neckWidthFactor, l = i * this.neckHeightFactor, c = T(t[t.length - 2], o, 0, i, !0), p = T(t[t.length - 2], o, 0, l, !0), f = T(o, c, Math.PI / 2, r, !1), m = T(o, c, Math.PI / 2, r, !0), v = T(o, p, Math.PI / 2, a, !1), P = T(o, p, Math.PI / 2, a, !0);
    return [v, f, o, m, P];
  }
  getPoints() {
    return this.points;
  }
  getBezierControlPointforGrowthAnimation() {
    return this.isClockWise ? {
      left: this.curveControlPointLeft,
      right: this.curveControlPointRight
    } : {
      right: this.curveControlPointLeft,
      left: this.curveControlPointRight
    };
  }
}
class en extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "freehand");
    this.cesium = t, this.freehand = !0, this.setState("drawing");
  }
  getType() {
    return "line";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length < 2 ? this.onMouseMove() : this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    this.points.push(t), this.setGeometryPoints(this.points), this.drawLine(), this.eventDispatcher.dispatchEvent("drawUpdate", t);
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t, this.setGeometryPoints(this.points), this.drawLine();
  }
  getPoints() {
    return this.points;
  }
}
class nn extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "freehand");
    this.cesium = t, this.freehand = !0, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length > 2 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    this.points.push(t), this.points.length > 2 && (this.setGeometryPoints(this.points), this.drawPolygon(), this.eventDispatcher.dispatchEvent("drawUpdate", t));
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t, this.setGeometryPoints(this.points), this.drawPolygon();
  }
  getPoints() {
    return this.points;
  }
}
class sn extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "arrowLengthScale", 5);
    g(this, "maxArrowLength", 3e6);
    g(this, "t");
    this.cesium = t, this.t = 0.3, this.setState("drawing"), this.onDoubleClick();
  }
  getType() {
    return "line";
  }
  /**
   * Points are only added upon click events.
   */
  addPoint(t) {
    this.points.push(t), this.points.length < 2 ? this.onMouseMove() : this.points.length === 2 && (this.setGeometryPoints(this.points), this.drawLine());
  }
  /**
   * Draw the shape based on the mouse movement position during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    let i = [];
    e.length === 2 ? (this.setGeometryPoints(e), this.drawLine()) : (i = this.createGraphic(e), this.setGeometryPoints(i));
  }
  /**
   * During editing mode, drag key points to update the corresponding data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawLine();
  }
  /**
   * Generate geometric shape points based on key points..
   */
  createGraphic(t) {
    const e = t.map((a) => this.cartesianToLnglat(a)), i = At(this.t, e), o = [].concat(...i);
    return this.cesium.Cartesian3.fromDegreesArray(o);
  }
  getPoints() {
    return this.points;
  }
}
class on extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "freehand");
    this.cesium = t, this.freehand = !0, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length > 1 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t], i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  createGraphic(t) {
    const e = t.map((m) => this.cartesianToLnglat(m)), i = e[0], o = e[1], r = O(i, o), a = Math.abs((i[0] - o[0]) / 2), l = Math.abs((i[1] - o[1]) / 2), c = this.generatePoints(r, a, l), p = [].concat(...c);
    return this.cesium.Cartesian3.fromDegreesArray(p);
  }
  generatePoints(t, e, i) {
    let [o, r, a, l] = [null, null, 0, []];
    for (let c = 0; c <= 100; c++)
      a = Math.PI * 2 * c / 100, o = t[0] + e * Math.cos(a), r = t[1] + i * Math.sin(a), l.push([o, r]);
    return l;
  }
  getPoints() {
    return this.points;
  }
}
class rn extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "freehand");
    this.cesium = t, this.freehand = !0, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length === 2 || this.points.length > 2 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t], i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  createGraphic(t) {
    const e = t.map((M) => this.cartesianToLnglat(M));
    if (e.length === 2) {
      const M = O(e[0], e[1]), S = A(e[0], M), C = T(e[0], M, Math.PI / 2, S, !1);
      e.push(C);
    }
    let [i, o, r, a, l] = [
      e[0],
      e[1],
      e[2],
      void 0,
      void 0
    ];
    const c = Ui(i, o, r), p = A(i, c), f = q(i, c), m = q(o, c);
    Z(i, o, r) ? (a = m, l = f) : (a = f, l = m);
    let v = ce(c, p, a, l);
    const P = [].concat(...v);
    return this.cesium.Cartesian3.fromDegreesArray(P);
  }
  getPoints() {
    return this.points;
  }
}
class an extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    this.cesium = t, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length > 1 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t], i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  createGraphic(t) {
    const [e, i] = t.map(this.cartesianToLnglat), o = [...e, e[0], i[1], ...i, i[0], e[1], ...e];
    return this.cesium.Cartesian3.fromDegreesArray(o);
  }
  getPoints() {
    return this.points;
  }
}
class hn extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    this.cesium = t, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length === 3 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    this.setGeometryPoints(e), e.length === 2 ? this.addTempLine() : (this.removeTempLine(), this.drawPolygon());
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t, this.setGeometryPoints(this.points), this.drawPolygon();
  }
  getPoints() {
    return this.points;
  }
}
class cn extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    this.cesium = t, this.setState("drawing"), this.onDoubleClick();
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 && this.onMouseMove();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    this.setGeometryPoints(e), e.length === 2 ? this.addTempLine() : (this.removeTempLine(), this.drawPolygon());
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t, this.setGeometryPoints(this.points), this.drawPolygon();
  }
  getPoints() {
    return this.points;
  }
}
class ln extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    g(this, "freehand");
    this.cesium = t, this.freehand = !0, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length > 1 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t], i = this.createGraphic(e);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  createGraphic(t) {
    const e = t.map((p) => this.cartesianToLnglat(p)), i = e[0], o = e[1], r = A(i, o), a = this.generatePoints(i, r), l = [].concat(...a);
    return this.cesium.Cartesian3.fromDegreesArray(l);
  }
  generatePoints(t, e) {
    let i, o, r;
    const a = [];
    for (let l = 0; l <= 100; l++)
      r = Math.PI * 2 * l / 100, i = t[0] + e * Math.cos(r), o = t[1] + e * Math.sin(r), a.push([i, o]);
    return a;
  }
  getPoints() {
    return this.points;
  }
}
class un extends k {
  constructor(t, e, i) {
    super(t, e, i);
    g(this, "points", []);
    this.cesium = t, this.setState("drawing");
  }
  getType() {
    return "polygon";
  }
  /**
   * Add points only on click events
   */
  addPoint(t) {
    this.points.push(t), this.points.length === 1 ? this.onMouseMove() : this.points.length === 3 && this.finishDrawing();
  }
  /**
   * Draw a shape based on mouse movement points during the initial drawing.
   */
  updateMovingPoint(t) {
    const e = [...this.points, t];
    if (this.setGeometryPoints(e), e.length === 2)
      this.addTempLine();
    else {
      this.removeTempLine();
      const i = this.createGraphic(e);
      this.setGeometryPoints(i), this.drawPolygon();
    }
  }
  createGraphic(t) {
    const e = t.map((v) => this.cartesianToLnglat(v)), [i, o, r] = [e[0], e[1], e[2]], a = A(o, i), l = q(o, i), c = q(r, i), p = ce(i, a, l, c);
    p.push(i, p[0]);
    const f = [].concat(...p);
    return this.cesium.Cartesian3.fromDegreesArray(f);
  }
  /**
   * In edit mode, drag key points to update corresponding key point data.
   */
  updateDraggingPoint(t, e) {
    this.points[e] = t;
    const i = this.createGraphic(this.points);
    this.setGeometryPoints(i), this.drawPolygon();
  }
  getPoints() {
    return this.points;
  }
}
const he = {
  FineArrow: le,
  AttackArrow: Lt,
  SwallowtailAttackArrow: zi,
  SquadCombat: ue,
  SwallowtailSquadCombat: $i,
  StraightArrow: Ji,
  CurvedArrow: Qi,
  AssaultDirection: Zi,
  DoubleArrow: tn,
  FreehandLine: en,
  FreehandPolygon: nn,
  Curve: sn,
  Ellipse: on,
  Lune: rn,
  Reactangle: an,
  Triangle: hn,
  Polygon: cn,
  Circle: ln,
  Sector: un
};
he.createGeometryFromData = (h, s, t) => {
  const { type: e, style: i, cartesianPoints: o } = t, r = new he[e](h, s, i);
  r.points = o;
  const a = r.createGraphic(o);
  return r.setGeometryPoints(a), r.type == "polygon" ? r.drawPolygon() : r.drawLine(), r.finishDrawing(), r.onClick(), r;
};
export {
  he as default
};
